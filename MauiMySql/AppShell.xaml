<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiMySql.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MauiMySql.Views"
    Shell.NavBarIsVisible="False">

    <!-- 👇 Estas rutas no se muestran como Tabs, pero sí permiten navegar -->
    <ShellContent Route="login" ContentTemplate="{DataTemplate views:LoginView}" />


    <TabBar
        Title="MainTabs"
        Shell.TabBarBackgroundColor="White"
        Shell.TabBarForegroundColor="Black"
        Shell.TabBarTitleColor="Black"
        Shell.TabBarUnselectedColor="Gray">

        <Tab Title="Inicio" Icon="icon_home.png">
            <ShellContent Route="home" ContentTemplate="{DataTemplate views:AgregarView}" />
        </Tab>

        <Tab Title="Carrito" Icon="icon_cart.png">
            <ShellContent Route="carrito" ContentTemplate="{DataTemplate views:CarritoView}" />
        </Tab>
        <Tab Title="Configuración" Icon="icon_config.png">
            <ShellContent ContentTemplate="{DataTemplate views:ConfiguracionView}" />
        </Tab>


    </TabBar>
</Shell>
