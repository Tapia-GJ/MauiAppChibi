<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiMySql.Views.LoginView"
             Title="Acceso"
             BackgroundColor="#f8fafc">

    <ScrollView>
        <VerticalStackLayout Padding="40" Spacing="30" VerticalOptions="Center" MinimumHeightRequest="800">
            <!-- Mensaje de error elegante -->
            <Frame BackgroundColor="#fef2f2" 
       CornerRadius="15" 
       HasShadow="False"
       Padding="20"
       BorderColor="#fecaca"
       x:Name="ErrorFrame"
       IsVisible="False">
                <StackLayout Orientation="Horizontal" Spacing="10">
                    <Label Text="⚠️"
               FontSize="20"
               VerticalOptions="Center"/>
                    <Label x:Name="MensajeError"
               TextColor="#dc2626"
               FontSize="16"
               FontAttributes="None"
               VerticalOptions="Center"
               HorizontalOptions="FillAndExpand"
               LineBreakMode="WordWrap"/>
                </StackLayout>
            </Frame>
            <!-- Formulario de login -->
            <Frame BackgroundColor="White" 
                   CornerRadius="25" 
                   HasShadow="True"
                   Padding="35"
                   Margin="0,0,0,20">
                <Frame.Shadow>
                    <Shadow Brush="#cbd5e1" Offset="0,8" Radius="25" Opacity="0.15"/>
                </Frame.Shadow>

                <VerticalStackLayout Spacing="25">

                    <!-- Campo Email -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="📧 Correo electrónico"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#374151"/>
                        <Frame BackgroundColor="#f8fafc" 
                               CornerRadius="15" 
                               HasShadow="False"
                               Padding="0"
                               BorderColor="#e5e7eb">
                            <Entry x:Name="EmailEntry"
                                   Placeholder="tu@email.com"
                                   BackgroundColor="Transparent"
                                   TextColor="#1f2937"
                                   PlaceholderColor="#9ca3af"
                                   Keyboard="Email"
                                   FontSize="16"
                                   HeightRequest="55"
                                   Margin="20,0"
                                   ClearButtonVisibility="WhileEditing" />
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Campo Contraseña -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="🔒 Contraseña"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#374151"/>
                        <Frame BackgroundColor="#f8fafc" 
                               CornerRadius="15" 
                               HasShadow="False"
                               Padding="0"
                               BorderColor="#e5e7eb">
                            <Entry x:Name="PasswordEntry"
                                   Placeholder="••••••••"
                                   BackgroundColor="Transparent"
                                   TextColor="#1f2937"
                                   PlaceholderColor="#9ca3af"
                                   IsPassword="True"
                                   FontSize="16"
                                   HeightRequest="55"
                                   Margin="20,0"
                                   ClearButtonVisibility="WhileEditing" />
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Botón Iniciar Sesión -->
                    <Frame CornerRadius="18" 
                           HasShadow="True"
                           Margin="0,15,0,0"
                           Padding="0">
                        <Frame.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#059669" Offset="0"/>
                                <GradientStop Color="#047857" Offset="0.5"/>
                                <GradientStop Color="#065f46" Offset="1"/>
                            </LinearGradientBrush>
                        </Frame.Background>
                        <Frame.Shadow>
                            <Shadow Brush="#059669" Offset="0,6" Radius="20" Opacity="0.3"/>
                        </Frame.Shadow>
                        <Button Text="🚀 Iniciar sesión"
                                Clicked="IniciarSesion_Clicked"
                                BackgroundColor="Transparent"
                                TextColor="White"
                                FontSize="18"
                                FontAttributes="Bold"
                                HeightRequest="60" />
                    </Frame>

                    <!-- Divisor elegante -->
                    <StackLayout Orientation="Horizontal" 
                                 HorizontalOptions="FillAndExpand"
                                 Margin="0,10">
                        <BoxView BackgroundColor="#e5e7eb" 
                                 HeightRequest="1" 
                                 HorizontalOptions="FillAndExpand" 
                                 VerticalOptions="Center"/>
                        <Label Text="  o  " 
                               TextColor="#9ca3af" 
                               FontSize="14"
                               VerticalOptions="Center"/>
                        <BoxView BackgroundColor="#e5e7eb" 
                                 HeightRequest="1" 
                                 HorizontalOptions="FillAndExpand" 
                                 VerticalOptions="Center"/>
                    </StackLayout>

                    <!-- Botón Registrarse -->
                    <Frame CornerRadius="18" 
                           HasShadow="True"
                           Padding="0">
                        <Frame.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#2563eb" Offset="0"/>
                                <GradientStop Color="#1d4ed8" Offset="0.5"/>
                                <GradientStop Color="#1e40af" Offset="1"/>
                            </LinearGradientBrush>
                        </Frame.Background>
                        <Frame.Shadow>
                            <Shadow Brush="#2563eb" Offset="0,6" Radius="20" Opacity="0.3"/>
                        </Frame.Shadow>
                        <Button Text="✨ Crear cuenta nueva"
                                Clicked="Registrarse_Clicked"
                                BackgroundColor="Transparent"
                                TextColor="White"
                                FontSize="18"
                                FontAttributes="Bold"
                                HeightRequest="60" />
                    </Frame>

                </VerticalStackLayout>
            </Frame>

            

            <!-- Footer minimalista -->
            <Label Text="Hecho con ❤️ para foodies"
                   FontSize="14"
                   TextColor="#9ca3af"
                   HorizontalOptions="Center"
                   Margin="0,30,0,20"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>